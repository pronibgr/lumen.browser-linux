#pragma once

// Forward to storage/database.hpp
#include "storage/database.hpp"

namespace Blueprint::Core {
    // Alias namespace for convenience if referenced as Core::Database
    namespace Storage = Blueprint::Storage;
}
